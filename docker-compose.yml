version: '2'
services:
  web:
    build: .
    restart: always
  volumes:
   - "./packagecache:/var/cache/pacman/aurto:rw"
  repo-server:
    image: nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
     - "./packagecache:/usr/share/nginx/html:ro"
